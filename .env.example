# CRM Quality Inspector — production environment
# Copy to .env and fill in values. NEVER commit this file.

# Connect to the NATIVE PostgreSQL on the Ollama server (port 5432)
DATABASE_URL=postgres://crm_quality:VZ3bfcIXzJqVfgo1L1k5Lmrhj3HI4z@127.0.0.1:5432/crm_quality_inspector

# Admin credentials
ADMIN_USERNAME=admin
ADMIN_PASSWORD=knVnXLtwmtKEG3wgG60HnsI

# Server binding
SERVER_HOST=0.0.0.0
SERVER_PORT=3000

# Log file inside the container (mounted volume on host)
LOG_FILE=/var/log/crm-qi/crm-quality-inspector.log
